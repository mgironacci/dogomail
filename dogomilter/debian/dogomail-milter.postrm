#!/bin/sh

set -e

if [ "$1" = "purge" ] ; then
  for dir in /etc/dogomail/ /var/log/dogomail; do
    if [ -d "$dir" ]; then
      rmdir "$dir" --ignore-fail-on-non-empty || true
    fi
  done
fi


#DEBHELPER#

exit 0

